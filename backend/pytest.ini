[pytest]
# Pytest configuration file

# Register custom markers
markers =
    integration: marks tests as integration tests (real API calls, slower)

# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
addopts = 
    --strict-markers
    --verbose

